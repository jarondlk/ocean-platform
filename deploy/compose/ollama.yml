# Optional local Ollama service overlay.
services:
  ollama:
    image: ollama/ollama:latest
    container_name: ocean_ollama
    restart: unless-stopped
    ports:
      - "127.0.0.1:${OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama:/root/.ollama

volumes:
  ollama:
